These are the modules needed to analyse/compare the
profiling data contained in the resulting .xml files.

Be sure to have .Net Framework 3.5 installed along with
the chart control. Otherwise the application may crash.

Browse to the xml file and operate on the discovered measures.